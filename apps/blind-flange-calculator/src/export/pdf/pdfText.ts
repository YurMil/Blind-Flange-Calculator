// Canonical implementation lives in `src/pdfText.ts`. Re-exported here so
// report builders under `src/export/pdf/` have a local, discoverable import.
export {sanitizePdfText} from '../../pdfText';
